version: 2

sphinx:
  fail_on_warning: true

conda:
  environment: environment.yml

python:
  # Install our python package before building the docs
  install:
    - method: pip
      path: .

formats:
  - pdf
  - epub
